namespace CONST {
  export const GAME_WIDTH: number = 400;
  export const GAME_HEIGHT: number = 600;

  export const LAYERS = {
    DEFAULT: 0,
    BACKGROUND: -10,
    FRONT: 10,
    TEXT: 100,
  };

  export const INPUT = {
    SMALL_TIME_INTERVAL_MS: 200,
  };
}
