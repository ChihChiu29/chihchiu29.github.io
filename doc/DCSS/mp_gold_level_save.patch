diff --git a/crawl-ref/source/end.cc b/crawl-ref/source/end.cc
index 8109dfd408..4ff8e484df 100644
--- a/crawl-ref/source/end.cc
+++ b/crawl-ref/source/end.cc
@@ -6,6 +6,7 @@
 #include "AppHdr.h"
 
 #include "end.h"
+#include "package.h"
 
 #include <cerrno>
 
@@ -209,12 +210,18 @@ NORETURN void end(int exit_code, bool print_error, const char *format, ...)
 
 // Delete save files on game end.
 // Non-static for catch2-tests.
-void delete_files()
+void delete_files(bool keep_save)
 {
     crawl_state.need_save = false;
-    you.save->unlink();
-    delete you.save;
-    you.save = 0;
+    if (you.save)
+    {
+        if (keep_save)
+            you.save->abort();
+        else
+            you.save->unlink();
+        delete you.save;
+        you.save = 0;
+    }
 }
 
 NORETURN void screen_end_game(string text, game_exit exit)
@@ -223,7 +230,7 @@ NORETURN void screen_end_game(string text, game_exit exit)
     tiles.send_exit_reason("quit");
 #endif
     crawl_state.cancel_cmd_all();
-    delete_files();
+    delete_files(exit == game_exit::death);
 
     if (!text.empty())
         ui::message(text, "", "<cyan>Hit any key to exit...</cyan>", true);
@@ -308,7 +315,7 @@ NORETURN void end_game(scorefile_entry &se)
 
     identify_inventory();
 
-    delete_files();
+    delete_files(!non_death);
 
     string fname = morgue_name(you.your_name, se.get_death_time());
     if (!dump_char(fname, true, true, &se))
diff --git a/crawl-ref/source/end.h b/crawl-ref/source/end.h
index 157579fc72..058a8602ca 100644
--- a/crawl-ref/source/end.h
+++ b/crawl-ref/source/end.h
@@ -27,4 +27,4 @@ struct game_ended_condition : public exception
 };
 
 // Available for catch2-tests
-void delete_files();
+void delete_files(bool keep_save = false);
diff --git a/crawl-ref/source/player.cc b/crawl-ref/source/player.cc
index 982b6512f8..abd86839c4 100644
--- a/crawl-ref/source/player.cc
+++ b/crawl-ref/source/player.cc
@@ -3032,8 +3032,8 @@ void level_change(bool skip_attribute_increase)
             }
 
             const bool manual_stat_level = you.has_mutation(MUT_DIVINE_ATTRS)
-                ? (new_exp % 3 == 0)  // 3,6,9,12...
-                : (new_exp % 6 == 3); // 3,9,15,21,27
+                ? (new_exp % 2 == 0)  // 3,6,9,12...
+                : (new_exp % 4 == 2); // 3,9,15,21,27
 
             // Must do this before actually changing experience_level,
             // so we will re-prompt on load if a hup is received.
@@ -3921,7 +3921,8 @@ static void _dec_mp(int mp_loss, bool silent)
     if (mp_loss < 1)
         return;
 
-    you.magic_points -= mp_loss;
+    // you.magic_points -= mp_loss;
+    you.magic_points -= 0;
 
     you.magic_points = max(0, you.magic_points);
     if (!silent)
@@ -5450,7 +5451,7 @@ player::player()
     total_experience = 0;
     experience_level = 1;
     experience_pool  = 0;
-    gold             = 0;
+    gold             = 10000;
     zigs_completed   = 0;
     zig_max          = 0;
 
